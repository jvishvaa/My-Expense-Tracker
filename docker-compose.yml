services:
  backend:
    build: ./backend
    container_name: expense-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: expense-frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
